@extends('layouts.admin')
@section('page-title','Dataset')
@section('content')
<div class="card p-4"><div class="d-flex flex-wrap gap-2 justify-content-between align-items-center mb-3"><form class="d-flex gap-2"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Cari dataset..."><button class="btn btn-outline-primary">Cari</button></form><a href="{{ route('admin.datasets.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>Tambah Dataset</a></div>
<div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Dataset</th><th>Kategori</th><th>Instansi</th><th>Tahun</th><th>Status</th><th></th></tr></thead><tbody>@forelse($datasets as $d)<tr><td><div class="fw-semibold">{{ $d->title }}</div><small class="text-muted">{{ $d->format }} · {{ number_format($d->downloads) }} download</small></td><td>{{ $d->category->name }}</td><td>{{ $d->organization->name }}</td><td>{{ $d->year }}</td><td><span class="badge {{ $d->status==='published'?'bg-success-subtle text-success':'bg-secondary-subtle text-secondary' }}">{{ $d->status }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.datasets.edit',$d) }}"><i class="bi bi-pencil"></i></a> <form class="d-inline" method="post" action="{{ route('admin.datasets.destroy',$d) }}" onsubmit="return confirm('Hapus dataset?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">Belum ada dataset.</td></tr>@endforelse</tbody></table></div>{{ $datasets->links() }}</div>
@endsection