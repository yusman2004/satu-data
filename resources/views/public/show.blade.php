@extends('layouts.public')
@section('content')
<div class="container py-5"><a href="{{ route('home') }}" class="text-decoration-none"><i class="bi bi-arrow-left me-2"></i>Kembali ke dataset</a>
<div class="row g-4 mt-2"><div class="col-lg-8"><div class="card p-4 p-lg-5"><span class="badge badge-soft rounded-pill align-self-start mb-3">{{ $dataset->category->name }}</span><h1 class="fw-bold">{{ $dataset->title }}</h1><p class="lead text-muted mt-3">{{ $dataset->description }}</p><hr>
<div class="row g-4"><div class="col-sm-6"><small class="text-muted">Instansi</small><div class="fw-semibold">{{ $dataset->organization->name }}</div></div><div class="col-sm-6"><small class="text-muted">Tahun</small><div class="fw-semibold">{{ $dataset->year }}</div></div><div class="col-sm-6"><small class="text-muted">Format</small><div class="fw-semibold">{{ $dataset->format }}</div></div><div class="col-sm-6"><small class="text-muted">Unduhan</small><div class="fw-semibold">{{ number_format($dataset->downloads) }}</div></div></div>
@if($dataset->metadata)<hr><h6 class="fw-bold">Metadata</h6><div class="row">@foreach($dataset->metadata as $k=>$v)<div class="col-md-4 mb-3"><small class="text-muted text-capitalize">{{ $k }}</small><div>{{ $v }}</div></div>@endforeach</div>@endif
</div></div><div class="col-lg-4"><div class="card p-4 sticky-lg-top" style="top:90px"><h5 class="fw-bold">Dataset</h5><p class="small text-muted">Gunakan dataset ini untuk analisis dan kebutuhan informasi publik.</p>@if($dataset->file_path)<a href="{{ route('dataset.download',$dataset) }}" class="btn btn-primary w-100"><i class="bi bi-download me-2"></i>Download Dataset</a>@else<div class="alert alert-info small mb-0">File belum tersedia. Silakan hubungi instansi penerbit.</div>@endif</div></div></div></div>
@endsection