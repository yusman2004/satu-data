CREATE DATABASE IF NOT EXISTS satu_data CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE satu_data;

-- Recommended: run Laravel migrations + seeder:
-- composer install
-- php artisan migrate --seed

-- The Laravel migrations are included in database/migrations.
